# Working notes for this repo

- Before telling the user to click/check/toggle something in a third-party UI (Spotify dashboard, Firebase console, GitHub settings, etc.), verify the actual current state/behavior of that control first — don't give a generic instruction and let the user discover it's wrong (e.g. telling them to check a checkbox that's actually disabled-by-default). Double-check instructions like this before handing them over.
